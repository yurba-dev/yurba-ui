{
    "name": "yurba-ui",
    "version": "1.0.0",
    "description": "Modal windows, tooltips, toasts, selects and dropdowns for Yurba.",
    "scripts": {
        "build": "node build.js"
    },
    "devDependencies": {
        "esbuild": "^0.24.0"
    }
}
